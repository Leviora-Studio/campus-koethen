export const USER_TEST_SOURCE = 'user-test';

/// Slug prefix that marks a public calendar as synthetic, so removal can find
/// exactly the seeded ones and never touch an editor's real calendar.
export const USER_TEST_CALENDAR_PREFIX = 'user-test-';
